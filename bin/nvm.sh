# Backward compatibility alias - nvm command points to unvm
# This file provides the nvm alias for users upgrading from older versions

# Source the main unvm.sh file
source "$(dirname "$0")/unvm.sh"
